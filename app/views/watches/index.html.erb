<p style="color: green"><%= notice %></p>

<% content_for :title, "Watches" %>

<h1>Watches</h1>

<div id="watches">
  <% @watches.each do |watch| %>
    <%= render watch %>
  <% end %>
</div>

<%= button_to "New watch", new_watch_path %>
